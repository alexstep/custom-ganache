{
  "name": "custom-ganache",
  "version": "1.0.0",
  "license": "MIT",
  "author": "to@alexstep.com",
  "keywords": ["ethereum", "ganache"],
  "description": "Run ganache testrpc from nodejs and your custom API",

  "engines": {
    "node": ">=10.8"
  },
  "engineStrict": true,

  "main": "index.mjs",

  "scripts": {
    "start": "node  --experimental-repl-await --harmony --experimental-modules cli.mjs",
    "dev": "nodemon --experimental-repl-await --harmony --experimental-modules index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  
  "dependencies": {
    "ganache-core": "^2.2.1"
  },
  "devDependencies": {
    "eslint": "^5.7.0",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-node": "^7.0.1",
    "eslint-plugin-promise": "^4.0.1",
    "eslint-plugin-standard": "^4.0.0",
    "nodemon": "^1.18.4"
  }
}
